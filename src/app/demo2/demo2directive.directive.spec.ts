import { Demo2directiveDirective } from './demo2directive.directive';

describe('Demo2directiveDirective', () => {
  it('should create an instance', () => {
    //const directive = new Demo2directiveDirective();
    //expect(directive).toBeTruthy();
  });
});
